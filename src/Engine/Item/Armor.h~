#include "VisibleItem.h"


#ifndef __ARMOR__
#define __ARMOR___
class Armor:public VisibleItem
{
public:
    Armor(ArmorInfo *info,Stats *BaseStats,string itemimg,AnimatedEntity *itementity);
    ArmorInfo* getInfo();

};

#endif
